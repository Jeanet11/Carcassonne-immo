{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-10 col-md-offset-1">

            <h2 class="col-md-12 text-left titre-annonces">Annonces</h2>
            <br>

                {{ form_start(form)}}

                <div class="row">
                    <div class="col-md-4">

                        <div id="custom-search-input">
                            <div class="input-group col-md-12 annonce-recherche">

                                {{ form_widget(form.search, {'attr': {'class': 'form-control input-sm',
                                                               'placeholder': 'Rechercher par titre'}}
                                ) }}
                                <span class="input-group-btn">
                                    <button class="btn btn-success btn-sm" type="submit">
                                        <i class="glyphicon glyphicon-search"></i>
                                    </button>
                                </span>
                            </div>
                        </div>
                    </div>
                </div>
            </br>
            {{ form_end(form)}}
            {# 
{{ form_start(form2)}}
{{ form_widget(form2)}}
{{ form_end(form2)}} #}
            {# <div class="row">
                <div >
                    <table class="table table-hover">
                        <thead>
                            <tr class="row">
                                <th class="col-md-7">Titre</th>
                                <th class="col-md-1 col-md-offset-1 text-center">Prix</th>
                                <th class="col-md-3 text-center">Nombre de pièces</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for announce in announces %}
                                <tr class="row">
                                    <td class="col-md-7">
                                        <a href="{{ path('announce_show', { 'id': announce.id }) }}">{{ announce.title }}</a>
                                    </td>
                                    <td class="col-md-1 col-md-offset-1 text-right">
                                        {{announce.price ~ ' €' }}
                                    </td>

                                    <td class="col-md-3 text-center">
                                        {{announce.numberOfRooms }}
                                    </td>

                                </tr>
                            {% endfor %}
                        </tbody>

                    </table>
                </div> #}
            </div>
        </div>
    </div>

    <div class="row">
        {% for announce in announces %}
            <div class=" announce-list panel panel-default row col-md-8 col-md-offset-2">
<p class="col-md-8 ann">
    <a href="{{ path('announce_show', { 'id': announce.id }) }}">{{ announce.title }}</a>
</p>
                <p class="col-md-2 ann">{{announce.numberOfRooms ~ ' pièces' }}</p>
                <p class="price col-md-2 text-center ann">{{announce.price ~ ' €'}}</p>

            </div>
        {% endfor %}
    </div>

{% endblock %}